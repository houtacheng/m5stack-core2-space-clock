#pragma once

// Optional first-boot Wi-Fi fallback. Copy this file to wifi_secrets.h and
// replace the sample values. wifi_secrets.h is intentionally not committed.
#define DEFAULT_WIFI_SSID ""
#define DEFAULT_WIFI_PASSWORD ""
